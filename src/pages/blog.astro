---
import BaseLayout from "../components/BaseLayout.astro";
import BlogPost from "../components/BlogPost.astro";
import BlogTaglist from "../components/BlogTaglist.astro";
import rss, { pagesGlobToRssItems } from '@astrojs/rss';
import { getCollection } from "astro:content";

const blogPosts = await getCollection('blog')
// Embed information
const pageTitle = "ame / blog"
const description = "buh"
---

<BaseLayout pageTitle={pageTitle}>
    <BlogTaglist />
    {
        blogPosts.map((post) => <BlogPost id={post.id} title={post.data.title} description={post.data.description} date={post.data.pubDate} tags={post.data.tags} />)
    }
</BaseLayout>
